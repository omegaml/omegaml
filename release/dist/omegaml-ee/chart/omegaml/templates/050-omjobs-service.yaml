apiVersion: v1
kind: Service
metadata:
  name: omjobs

spec:
  ports:
  - port: 5000
    targetPort: 5000
    name: jyhub
  - port: 8081
    targetPort: 8081
    name: jyhubspawner
  selector:
    io.kompose.service: omjobs
