{{- if  and .Values.localVolumes.enabled .Values.mysql.persistence.enabled}}
# https://vocon-it.com/2018/12/20/kubernetes-local-persistent-volumes/
apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-disk
spec:
  capacity:
    storage: {{ .Values.mysql.persistence.size }}
  # volumeMode field requires BlockVolume Alpha feature gate to be enabled.
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: {{ .Values.mysql.persistence.storageClass }} 
  local:
    path: {{ .Values.mysql.persistence.localVolumePath }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        {{- range $key, $val := .Values.mysql.nodeSelector }}
        - key: {{ $key }}
          operator: In
          values:
          - {{ $val }}
        {{- end }}
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{ .Values.mysql.persistence.storageClass }}
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: WaitForFirstConsumer
{{- end -}}

